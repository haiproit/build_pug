# build_pug
## Build Setup:

``` bash
# Download repository:
git clone git@github.com:haiproit/build_pug.git

# Go to the app:
cd build_pug

# Install dependencies:
npm i

# Server with hot reload at http://localhost:8081/
npm run dev

# Output will be at dist/ folder
npm run build
```
